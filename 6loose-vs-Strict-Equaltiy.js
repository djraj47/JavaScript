// Loose Equalty(==) VS Strict Equality operato (===)

let age = 24;

// Loose Equality 
console.log(age == "24"); // focus only on value but not datatype

// Strict Equality
console.log(age === "24") // foucs on both value and datatype