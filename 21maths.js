// maths object 
console.log(Math);

let number = 7.6;
console.log(Math.round(number)); // round off
console.log(Math.floor(number)); // round off to down
console.log(Math.ceil(number)); // round off to up
console.log(Math.trunc(number)); // remove decimal part
console.log(Math.random()); // gives a random value